# /summit-context-refresh

Refresh the compressed context packet for the current project.

## Workflow

1. Confirm `scripts/context_engine.py` exists.
2. Run `python3 scripts/context_engine.py refresh --source manual`.
3. Summarize the new next-best-step from `.codex-loop/context/handoff.md`.
